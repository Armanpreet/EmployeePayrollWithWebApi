@model IEnumerable<EmployeePayrollWithWebApi.Models.Designation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DesiName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DesiName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DesignId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DesignId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DesignId })
        </td>
    </tr>
}

</table>
